What to test: Adding packages from custom repositories.

How to test:
Build the image and unpack tar.gz to the target rootfs, boot it.

Expected result:
Docker and docker-compose tools should present on target.
